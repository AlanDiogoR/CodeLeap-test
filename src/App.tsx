import { type ReactElement } from 'react'

function App(): ReactElement {
  return (
    <div className="min-h-screen bg-background p-4">
      <h1 className="font-bold text-2xl text-primary">CodeLeap Network</h1>
      <p className="mt-4 text-muted">
        Base estrutural configurada. Edite src/App.tsx para começar.
      </p>
    </div>
  )
}

export default App
